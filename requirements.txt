# Source of truth for pinned versions. Frozen from the working .venv.
# Install with:  pip install -r requirements.txt
# pandas / streamlit are held below their latest pins by hopsworks 5.0.6
# (needs pandas<2.4, protobuf<5); tensorflow was dropped in Phase 4.5 (unused,
# and its protobuf>=6.31.1 pin is irreconcilable with hopsworks). See PROGRESS.md.
pandas==2.3.3
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.3.0
torch==2.13.0
shap==0.52.0
statsmodels==0.15.0
openmeteo_requests==1.7.5
requests==2.34.2
requests_cache==1.3.3
matplotlib==3.11.1
python-dotenv==1.2.3
pyarrow==25.0.1
streamlit==1.59.1
hopsworks[python]==5.0.6
# delta-rs: hopsworks needs a delta lib for non-Spark ops and ships hops-deltalake
# for it, but that wheel is linux/macos-arm only, so use upstream deltalake.
deltalake==1.6.3
# twofish (transitive via pyjks via hopsworks) ships only an sdist; on Windows
# with no C compiler the build fails. This local shim satisfies the import; the
# JKS-decrypt path that would actually use the cipher is never hit by API-key
# login. On Linux/macOS the real sdist builds fine, so the marker skips the shim.
./vendor/twofish ; platform_system == "Windows"

